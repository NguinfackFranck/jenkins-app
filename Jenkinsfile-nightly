pipeline {
    agent any

    stages {
        stage('Build Docker Images') {
            steps {
                sh 'docker build -f ci/Dockerfile-playright -t my-docker  .'
                sh 'docker build -f ci/Dockerfile-aws-cli -t my-aws-cli:latest  .'
            }
        }
     }
}